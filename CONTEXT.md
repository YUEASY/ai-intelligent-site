# AI 智能建站

曜势科技"AI 智能建站"项目的领域术语表：给中国跨境卖家建"会自己运营的海外独立站"，自研 AI 中台完成商品上架、SEO 优化、用户行为分析与客服。

## Language

**商户 (Merchant)**:
曜势科技的付费客户，即经营海外独立站的跨境卖家。
_Avoid_: 客户（易与访客混淆）、用户、卖家

**访客 (Visitor)**:
在商户独立站上浏览、搜索、加购、下单的海外终端消费者。
_Avoid_: 用户、客户、买家

**智能体 (Agent)**:
可自主进行多步推理与决策的 AI 单元，例如 SEO Agent、商品 Agent。
_Avoid_: 工作流、模型（泛指时）

**工作流 (Workflow)**:
由代码确定性编排的固定业务流程，无自主决策。
_Avoid_: 智能体

**商品标准模型 (Canonical Product)**:
各来源商品归一化后的内部标准结构，AI 商品处理与写入 Shopify 均基于它。
_Avoid_: 原始商品行、CSV 原始字段

**导入适配器 (Import Adapter)**:
把外部商品源（MVP 为 CSV，后续为 Amazon/Shopee）转换成商品标准模型的隔离层。
_Avoid_: 平台适配器（指 Shopify 侧的 Adapter，概念不同）

**草稿 (Draft)**:
尚未正式发布的商品或页面版本；AI 输出先写入 Draft，经人工确认后才发布。
_Avoid_: 待发布、临时数据

**页面 (Page)**:
商户店铺中独立于商品的内容页面，例如 About、Shipping；它拥有独立的发布、SEO 与版本生命周期。
_Avoid_: 商品页（属于商品）、网页（范围过宽）

**风险分级 (Risk Grading)**:
按"字段 + 操作类型"将 AI 操作划分为低/中/高风险，决定自动执行、人工确认或禁止；由确定性代码规则判定，不由 LLM 自评。
_Avoid_: LLM 判断自身风险

**回滚 (Rollback)**:
将商品/页面恢复到上一版本快照（Version N → N+1），并记录字段级 diff。
_Avoid_: 逐字段手工还原

**基准工时 (Baseline Effort)**:
每类 AI 任务的人工耗时基准，用于估算"节省人工小时数"；由启动时 5~10 个实测样本的中位数确定，MVP 轮内冻结不改。
_Avoid_: 拍脑袋估算、事后调口径

**冷启动 (Cold Start)**:
新站尚未积累 GSC 与行为数据时，用商品资料 + 基础 SEO 规则 + LLM 生成第一轮内容的阶段；待 GSC 产生真实数据后再转入效果驱动的持续优化。
_Avoid_: 无数据阶段（模糊表述）

**任务 (Task)**:
一次 AI 操作的完整生命周期，从创建到终态（published / rejected / failed / rolled_back）全程进入审计日志；状态由代码确定性推进，不由 LLM 控制。
_Avoid_: 作业、Job（泛指）

**审计日志 (Audit Log)**:
记录每次任务的状态迁移与回滚操作（操作者、时间、恢复版本、字段 Diff），是"可追溯、可回滚"承诺的实现载体。
_Avoid_: 应用日志、系统日志

**变体 (Variant)**:
商品的一个具体款式，含独立 SKU、选项（颜色/尺码等）、价格、成本、库存与图片；MVP 业务层限最多 2 个选项维度，底层模型可扩展。
_Avoid_: 规格、SKU（SKU 是变体的一个属性，而非变体本身）

**审核 (Review)**:
中/高风险任务发布前的人工确认关卡；结果分为通过、驳回（结构化填原因）、人工修改后发布。
_Avoid_: 审批（语义更宽）、校验（校验是确定性代码规则，审核是人）
